name: Test

on:
  workflow_dispatch:

jobs:
  buildAndTest:
    runs-on: arc-runner-set
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Dagger
        uses: ./.github/dagger

      - name: Release and deploy with Dagger
        run: dagger run go run ci/dotnet-test.go

      - name: Test Report
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: DotNET Tests
          path: "**/test-results.trx"
          reporter: dotnet-trx
          fail-on-error: true
