namespace Infrastructure.CDKTF.Environment.Local;

internal sealed class EnvironmentApp : HashiCorp.Cdktf.App
{
    public EnvironmentApp()
    {
        // new ServiceAStackSet(this, values, /** other configurations ** /);
    }
}